version: 2

jobs:
  build:
    machine:
      image: windows-default 
    resource_class: windows.medium
    steps:
      - run: systeminfo
      - run: wget https://mirror.koddos.net/apache/hadoop/common/hadoop-3.2.2/hadoop-3.2.2-src.tar.gz -O hadoop-3.2.2.tar.gz
      - run: tar -zxf hadoop-3.2.2.tar.gz
      - run: ls -lh
